// Função que navega para uma nova página ou rota, alterando o URL atual do navegador
function navigateTo(path) {
    // Define a URL da janela para o caminho informado, causando o carregamento da nova página
    window.location.href = path;
}
